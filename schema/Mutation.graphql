type Mutation {
    novoUsuario(
        nome: String!
        email: String!
        idade: Int
        salario: Float
        vip: Boolean
    ): Usuario

    deleteUsuario(
        id: ID!
    ): Usuario
}