type Mutation {
  novoUsuario(
    nome: String!
    email: String!
    senha: String!
    idade: Int
    salario: Float
    vip: Boolean
  ): Usuario

  deleteUsuario(id: ID!): Usuario

  updateUsuario(id: ID!, dados: UsuarioInput!): Usuario
}
